name: Deploy Cloudflare Pages
on:
  push:
    branches:
      - dev
      - master
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v6
        with:
          node-version: '22' # 降级到更稳定的 LTS 版本
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm install && pnpm build

      - name: Build & Deploy Worker
        id: cloudflare_deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # 修正为动态分支，避免 master 推送但在 CF 显示为 main
          command: pages deploy dist --project-name=${{ secrets.CF_PROJECT_NAME }} --branch=${{ github.ref_name }}
          packageManager: pnpm

      - name: Discord Notification
        if: always() # 确保失败也能收到通知告知你哪里错了
        uses: appleboy/discord-action@master
        with:
          webhook_url: ${{ secrets.DISCORD_WEBHOOK_URL }} # 关键修正：url 改为 webhook_url
          color: ${{ job.status == 'success' && '#0070f3' || '#ff0000' }}
          content: "🚀 **项目部署通知**"
          message: |
            **项目名称**: `${{ secrets.CF_PROJECT_NAME }}`
            **当前状态**: ${{ job.status == 'success' && '✅ SUCCESS' || '❌ FAILED' }}

            ---
            **📄 部署详情**
            > **分支**: `${{ github.ref_name }}`
            > **提交信息**: ${{ github.event.head_commit.message }}
            > **执行作者**: `${{ github.actor }}`

            **🔗 相关链接**
            [查看 GitHub Actions 日志](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
